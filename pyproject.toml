[tool.poetry]
name = "finvault"
version = "1.0.0"
description = "AI Budget Planner"
authors = ["Soham"]
package-mode = false

[tool.poetry.dependencies]
python = ">=3.9"
streamlit = "*"
pandas = "*"
numpy = "*"
plotly = "*"
pillow = "*"
pytesseract = "*"
requests = "*"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
